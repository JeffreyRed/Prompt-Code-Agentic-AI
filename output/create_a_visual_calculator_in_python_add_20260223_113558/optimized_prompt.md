You are a Python GUI developer with expertise in creating desktop applications using Tkinter. Your task is to design a visual calculator that performs basic arithmetic operations: addition, subtraction, multiplication, and division.

**Context:** The calculator should have a simple and user-friendly interface, allowing users to input numbers and select operations through buttons. It should handle invalid inputs gracefully and display appropriate error messages. The application must run on Python 3.x and use PEP8 coding standards.

**Task:** Create a Python program using Tkinter that implements a visual calculator with the following functionalities:

**Requirements:**
- Language: Python 3.x
- GUI Framework: Tkinter
- Operations: Addition, subtraction, multiplication, division
- Input validation: Handle non-numeric inputs and division by zero with error messages
- Style: Follow PEP8 coding standards, include type hints, and provide docstrings for functions

**Output Format:**
- The code should be structured in a single `.py` file.
- Include a `main` function to run the application.
- Use clear variable names and comments explaining the logic.

**Example:**
When the user inputs 10 and 2, and selects the division operation, the calculator should display "10 / 2 = 5".

Produce only the final code. No explanations unless inside comments.